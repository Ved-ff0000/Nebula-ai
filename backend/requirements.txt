# NEBULA backend requirements (Python 3.11+)
fastapi>=0.115
uvicorn[standard]>=0.30
SQLAlchemy>=2.0.30
pydantic>=2.7
pydantic-settings>=2.3
email-validator>=2.1
PyJWT>=2.8
httpx>=0.27
playwright>=1.45

# Required by FastAPI for the multipart form on the local demo site
# (app/services/demo_site.py uses Form(...)). Without it the demo pages used by
# the form-fill approval demo return 500.
python-multipart>=0.0.9

# Production database driver (docker-compose uses PostgreSQL).
# The codebase uses SQLAlchemy's *synchronous* engine, so the SQLite driver is
# the stdlib: aiosqlite/asyncpg are NOT used anywhere.
# psycopg2-binary>=2.9  → installed in the Docker image only
# (see docker/backend.Dockerfile)

# Testing
pytest>=8.2
pytest-asyncio>=0.23   # pytest.ini sets asyncio_mode = auto; the suite is async
