# NEBULA backend requirements (Python 3.11+)
fastapi>=0.115
uvicorn[standard]>=0.30
SQLAlchemy>=2.0.30
pydantic>=2.7
pydantic-settings>=2.3
email-validator>=2.1
PyJWT>=2.8
httpx>=0.27
playwright>=1.45

# Production database driver (docker-compose uses PostgreSQL)
# psycopg2-binary>=2.9  → installed in the Docker image only

# Testing
pytest>=8.2
